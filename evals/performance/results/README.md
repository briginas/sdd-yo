# Retained results

Generate each file with the create-only `--output` option documented in
[`Performance benchmarks`](../../../docs/performance-benchmarks.md). Do not
edit measured values or replace an existing result; retain a new report when
the source fingerprint or environment changes.
