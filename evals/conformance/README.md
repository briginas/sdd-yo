# Retained cross-platform conformance evidence

The `results/` directory retains the exact JSON artifacts produced by
[GitHub Actions run 30935118969](https://github.com/briginas/sdd-yo/actions/runs/30935118969)
for commit `23800ce0081363853f063654c495c44aeb8f1c59`.

The macOS, Linux, and Windows reports bind their environment and reviewed
source fingerprints. `results/summary.json` is the fail-closed aggregate and
records `MET` only because all three reports have identical source and
deterministic-manifest fingerprints. Do not edit measured values or replace
these files; retain a new result set for a different source or external run.
