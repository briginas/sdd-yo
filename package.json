{
  "name": "sdd-yo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "npm run clean:build && tsc -p tsconfig.build.json",
    "clean:build": "node -e \"import('node:fs/promises').then(({ rm }) => rm('dist', { recursive: true, force: true }))\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"test/**/*.ts\" \"*.md\" \"*.json\"",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "verify:stage-0": "node scripts/verify-stage-0.ts"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "json-schema-to-typescript": "15.0.4",
    "prettier": "3.6.2",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "remark-frontmatter": "5.0.0",
    "remark-gfm": "4.0.1",
    "remark-parse": "11.0.0",
    "saxes": "6.0.0",
    "unified": "11.0.5",
    "yaml": "2.9.0"
  }
}
